diff --git a/cube/swiss/source/devices/fat/deviceHandler-FAT.c b/cube/swiss/source/devices/fat/deviceHandler-FAT.c
index 2535a60..4dd1285 100644
--- a/cube/swiss/source/devices/fat/deviceHandler-FAT.c
+++ b/cube/swiss/source/devices/fat/deviceHandler-FAT.c
@@ -196,7 +196,9 @@ s32 deviceHandler_FAT_readFile(file_handle* file, void* buffer, u32 length) {
 s32 deviceHandler_FAT_writeFile(file_handle* file, const void* buffer, u32 length) {
 	if(!file->ffsFp) {
 		FFFIL* fp = malloc(sizeof(FFFIL));
-		if(f_open(fp, file->name, FA_CREATE_ALWAYS | FA_WRITE) != FR_OK) {
+		FRESULT dbg_opres = f_open(fp, file->name, FA_CREATE_ALWAYS | FA_WRITE);
+		if(dbg_opres != FR_OK) {
+			print_debug("DBG writeFile: f_open(%s) FAILED res=%i\n", file->name, dbg_opres);
 			free(fp);
 			return -1;
 		}
@@ -207,13 +209,20 @@ s32 deviceHandler_FAT_writeFile(file_handle* file, const void* buffer, u32 lengt
 		file->fileType  = IS_FILE;
 		file->blockSize = fatfs->ssize;
 		
-		f_expand(file->ffsFp, file->offset + length, 1);
+		FRESULT dbg_exres = f_expand(file->ffsFp, file->offset + length, 1);
+		if(dbg_exres != FR_OK)
+			print_debug("DBG writeFile: f_expand(%u) res=%i (informational)\n", (u32)(file->offset + length), dbg_exres);
 	}
-	f_lseek(file->ffsFp, file->offset);
+	FRESULT dbg_skres = f_lseek(file->ffsFp, file->offset);
+	if(dbg_skres != FR_OK)
+		print_debug("DBG writeFile: f_lseek(%u) res=%i\n", (u32)file->offset, dbg_skres);
 	
 	UINT bytes_written;
-	if(f_write(file->ffsFp, buffer, length, &bytes_written) != FR_OK) {
-		return -1;
+	FRESULT dbg_wrres = f_write(file->ffsFp, buffer, length, &bytes_written);
+	if(dbg_wrres != FR_OK || bytes_written != length) {
+		print_debug("DBG writeFile: f_write len=%u bw=%u res=%i\n", length, bytes_written, dbg_wrres);
+		if(dbg_wrres != FR_OK)
+			return -1;
 	}
 	file->offset = f_tell(file->ffsFp);
 	file->size = f_size(file->ffsFp);
@@ -369,6 +378,8 @@ s32 deviceHandler_FAT_closeFile(file_handle* file) {
 	int ret = 0;
 	if(file && file->ffsFp) {
 		ret = f_close(file->ffsFp);
+		if(ret != 0)
+			print_debug("DBG closeFile: f_close(%s) res=%i\n", file->name, ret);
 		free(file->ffsFp);
 		file->ffsFp = NULL;
 	}
@@ -387,7 +398,10 @@ s32 deviceHandler_FAT_deinit(file_handle* file) {
 
 s32 deviceHandler_FAT_deleteFile(file_handle* file) {
 	deviceHandler_FAT_closeFile(file);
-	return f_unlink(file->name);
+	int dbg_ulres = f_unlink(file->name);
+	if(dbg_ulres != 0)
+		print_debug("DBG deleteFile: f_unlink(%s) res=%i\n", file->name, dbg_ulres);
+	return dbg_ulres;
 }
 
 s32 deviceHandler_FAT_renameFile(file_handle* file, char* name) {
diff --git a/cube/swiss/source/fatfs/diskio.c b/cube/swiss/source/fatfs/diskio.c
index d50f534..12b1e74 100644
--- a/cube/swiss/source/fatfs/diskio.c
+++ b/cube/swiss/source/fatfs/diskio.c
@@ -8,6 +8,7 @@
 /*-----------------------------------------------------------------------*/
 
 #include "ff.h"			/* Basic definitions of FatFs */
+extern void print_debug(const char *fmt, ...);
 #include "diskio.h"		/* Declarations FatFs MAI */
 
 #include <sdcard/gcsd.h>
@@ -92,7 +93,10 @@ DRESULT disk_read (
 	if (!disc[pdrv])
 		return RES_NOTRDY;
 
-	return disc[pdrv]->vt->read_sectors(disc[pdrv], buff, sector, count, opt) ? RES_OK : RES_ERROR;
+	if (disc[pdrv]->vt->read_sectors(disc[pdrv], buff, sector, count, opt))
+		return RES_OK;
+	print_debug("DBG disk_read: pdrv=%u sec=%u cnt=%u FAILED\n", pdrv, (unsigned)sector, count);
+	return RES_ERROR;
 }
 
 
@@ -115,10 +119,15 @@ DRESULT disk_write (
 		return RES_PARERR;
 	if (!disc[pdrv])
 		return RES_NOTRDY;
-	if (!(disc[pdrv]->features & FEATURE_MEDIUM_CANWRITE))
+	if (!(disc[pdrv]->features & FEATURE_MEDIUM_CANWRITE)) {
+		print_debug("DBG disk_write: pdrv=%u WRPRT! features=%08x\n", pdrv, disc[pdrv]->features);
 		return RES_WRPRT;
+	}
 
-	return disc[pdrv]->vt->write_sectors(disc[pdrv], buff, sector, count, opt) ? RES_OK : RES_ERROR;
+	if (disc[pdrv]->vt->write_sectors(disc[pdrv], buff, sector, count, opt))
+		return RES_OK;
+	print_debug("DBG disk_write: pdrv=%u sec=%u cnt=%u FAILED\n", pdrv, (unsigned)sector, count);
+	return RES_ERROR;
 }
 
 
@@ -143,6 +152,8 @@ DRESULT disk_ioctl (
 	switch (ctrl) {
 		case CTRL_SYNC:
 			res = disc[pdrv]->vt->flush(disc[pdrv]) ? RES_OK : RES_ERROR;
+			if (res != RES_OK)
+				print_debug("DBG disk_ioctl: pdrv=%u CTRL_SYNC flush FAILED\n", pdrv);
 			break;
 
 		case GET_SECTOR_COUNT:
